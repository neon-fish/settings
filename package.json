{
  "name": "@neonfish/settings",
  "version": "0.1.0",
  "description": "Simple library to save and load settings to/from the user's home directory.",
  "author": "Neon Fish",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc -w",
    "build": "rimaf dist && tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.15.13",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "rxjs": "^7.8.0"
  },
  "files": [
    "dist"
  ]
}
